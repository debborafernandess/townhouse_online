<h1 class="ls-title-intro ls-ico-user">
  <%= t(controller.action_name).capitalize %>
</h1>

<%= form_for @resident do |f| %>
  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: 'form-control' %>
    <%= @resident.errors.messages.fetch(:name, nil) * ', ' %>
  </div>
  <div class="form-group">
    <%= f.label :document %>
    <%= f.text_field :document, class: 'form-control' %>
    <%= @resident.errors.messages.fetch(:document, nil) * ', ' %>
  </div>
   <div class="form-group">
    <%= f.label :email %>
    <%= f.text_field :email, class: 'form-control' %>
    <%= @resident.errors.messages.fetch(:email, nil) * ', ' %>
  </div>

  <div class="form-group">
    <%= link_to t('cancel'), :back, class: 'btn btn-default' %>
    <%= f.submit t('submit'), class: 'btn btn-primary' %>
  </div>
<% end %>
